using UnityEngine;

namespace _2Scripts.ScriptableObjects
{
    public class SO_DifficultyEditor : ScriptableObject
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
